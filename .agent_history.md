# Histórico de agente (orquestração)

## 2026-04-30 — Documentação: modo cluster / multi-worker

- **Fase:** documentação (Fase 6 / pedido explícito).
- **Decisão:** `continuar`.
- **Sumário:** `documentation.md` atualizado com secção “Modo cluster / multi-worker” (ativação vs sequencial, fases crawl→shard→download, exemplos de comandos, idempotência antes do sharding, diretórios temporários por worker, logging por worker, limites e recomendação de workers) e notas de integridade (`session.json.write.lock`, `manifest.json.queue.lock`, headless por defeito nos workers salvo `--headed`). `README.md` alinhado: Quick Start passa a usar `npm start -- --lessons`, bullets para `npm run all`, `--workers` e `--sequential`, mantendo o detalhe no doc mestre.
- **IDs / plano:** DOC-CLUSTER-1.

## 2026-04-29 — Cluster multi-worker (`main.js` + scrapers + manifest/sessão)

- **Fase:** execução (orquestrar / Fase 2 + verificação sintática).
- **Decisão:** `continuar`.
- **Sumário:** Modo cluster: crawl único (`discoverLessonTasks` / `discoverDocumentTasks`), filtro idempotente ao manifest, partição `partitionTasksForWorker`, `Promise.all` de N browsers com prefixos `chalk` (`workerLog.mjs`), `temp_downloads/worker-k` por worker, `launchBrowser({ preferHeadlessUnlessHeaded })` para workers (headless salvo `--headed`). Manifest: lock `.queue.lock` + cadeia por path; `session.json`: escrita atómica + `.write.lock`. Flags `--workers`, `--sequential`; sequencial preserva fluxo antigo. `documentation_3.md` inexistente no repo — convenções alinhadas a `documentation.md`.
- **IDs / plano:** CLUSTER-MAIN-1.

## 2026-04-29 — `documentation.md` alinhado a `src/` + `main.js`

- **Fase:** documentação (Fase 6 / pedido explícito).
- **Decisão:** `continuar`.
- **Sumário:** `documentation.md` reescrito: árvore `src/`, orquestrador Commander, scrapers, manifest, env documentos, `toMarkdown` desde `lessonScraper.js`, diagramas Mermaid e scripts npm. `package.json`: `stripper`/`documents` → `main.js --lessons`/`--docs`. `lessonScraper.js` restaurado após escrita acidental (conteúdo doc no path errado).
- **IDs / plano:** DOC-ARCH-DOC-1.

## 2026-04-29 — Modularização + `main.js` (orquestrador)

- **Fase:** execução (arquitetura / orquestrar).
- **Decisão:** `continuar`.
- **Sumário:** Código migrado para `src/scrapers/lessonScraper.js`, `src/scrapers/documentScraper.js`, utilitários em `src/utils/` (`infnetShared.mjs`, `downloadManifest.mjs`, `browser.mjs`, `logging.mjs`). `main.js` com Commander (`--all`, `--lessons`, `--docs`, `--fail-fast`, `ORCHESTRATOR_FAIL_FAST`), uma sessão Puppeteer partilhada, `browser.close()` em `finally`. `stripper.js` / `documentsScraper.js` mantidos como shims + reexport `toMarkdown`. Removidos `lib/*.mjs` (paths antigos). `package.json`: `start` → `main.js`, scripts `all`/`lessons`/`docs`; removida dependência espúria `"package.json"`.
- **IDs / plano:** ARCH-MAIN-1.

## 2026-04-29 — documentsScraper: paridade manifest

- **Fase:** execução (refino).
- **Decisão:** `continuar`.
- **Sumário:** `documentsScraper.js` já usava manifest; acrescentados `--ignore-manifest` / `DOCUMENTS_IGNORE_MANIFEST`, log `[MANIFEST]` no arranque, e `isItemCompleted(..., { verifyArtifact, manifestRoot })` para não omitir download se o registo existir mas o ficheiro tiver sido apagado. `stripper.js` e `lib/downloadManifest.mjs` alinhados à mesma verificação em disco.
- **IDs / plano:** MANIFEST-DOC-PARITY-1.

## 2026-04-29 — Manifest + idempotência (stripper + documentos)

- **Fase:** execução (Fase 2 orquestrar) + verificação sintática `node --check`.
- **Decisão:** `continuar`.
- **Sumário:** Novo `lib/downloadManifest.mjs` (`downloads/manifest.json` por defeito, paths relativos POSIX, IDs `transcript:gdrv:*` / hash de URL e `document:bp:*` / hash). Guardas antes de download; registo só após ficheiro final + `.md` (transcrições) ou ficheiro final (documentos), com verificação `fileExists`. Stripper: `--limit` conta só aulas efetivamente processadas (dry-run ou download novo); omitidas do manifest sem log por aula, sumário `[IDEMPOTÊNCIA]` no fim. Documentos: `resolveManifestRootAndPath` alinha manifest à raiz `downloads/` quando a saída está dentro dela.
- **IDs / plano:** MANIFEST-1 — persistência e idempotência.

## 2026-04-29 — Fase 6 / entrega documentação

- **Fase:** documentação (`documentation` skill, invocação `/orquestrar` focada em `documentation.md`).
- **Decisão:** `continuar`.
- **Sumário:** Criado `documentation.md` na raiz com propósito, arquitetura alinhada a `stripper.js`, diagramas Mermaid (visão do sistema, sequência, estado da sessão), referência CLI/env, saídas, `toMarkdown`, segurança, limitações e troubleshooting. README referenciado como entrada rápida; `VALIDACAO_SELETORES.md` documentado como opcional (listado em `.gitignore` no repo).
- **IDs / plano:** DOC-1 — documentação completa do projeto.

## 2026-04-29 — documentsScraper (BuddyPress Documentos)

- **Fase:** execução + verificação sintática (`node --check`).
- **Decisão:** `continuar`.
- **Sumário:** Novo `documentsScraper.js` (BFS em `/documents/` e `/documents/folders/:id/`), extração só por DOM (`li.download_file a`, pastas `ac-folder-list` + `a.media-folder_name`), downloads via `fetch` com cookies da página, streaming + `DOCUMENTS_MAX_BYTES`, dedupe de nomes, filtro default pdf/pptx/xlsx. Novo `lib/infnetShared.mjs` com auth/sessão reutilizável. `package.json` script `documents`; `.env.example` com variáveis opcionais.
- **IDs / plano:** DOC-SCRAPE-1 — módulo documentos Infnet.

## 2026-04-29 — Refactor stripper → infnetShared

- **Fase:** execução (deduplicação backend).
- **Decisão:** `continuar`.
- **Sumário:** `stripper.js` importa `lib/infnetShared.mjs` (sessão, login, Chrome, `ensureDir`/`fileExists`, `gotoUrl`). `gotoClasses` local delega `gotoUrl(..., 800)`; `ensureAuthenticated(..., { gotoSettleMs: 800 })` preserva pausa pós-navegação. `safeDirName` no shared ganhou `opts` (`maxLen`/`fallback`); stripper mantém 80 / `Disciplina`. `printChromeHelp` alinhado ao texto detalhado.
- **IDs / plano:** REF-SHARED-1 — DRY sessão Infnet.
